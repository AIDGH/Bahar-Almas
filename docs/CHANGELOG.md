# Changelog

## ۲۰۲۶-۰۹-۰۸ — بازی مهمان، پروفایل و دعوت

- انتقال ورود از ابتدای بازی به مرحله‌ی ثبت رکورد پس از پایان؛
- افزودن Claim Token Hash‌شده، مهلت ۲۴ ساعته و نگهداری نتیجه‌ی ثبت‌نشده روی همان Browser؛
- جداسازی کامل ورود و ثبت‌نام با OTP و پشتیبانی از نام‌های نمایشی تکراری؛
- افزودن پروفایل شامل ویرایش نام، شماره تماس، High Score، مجموع بازی‌ها، امتیاز دعوت، امتیاز کل و تاریخچه‌ی شمسی؛
- افزودن کد و لینک UTM یکتا و Reward تراکنشی ۱٬۰۰۰ امتیازی برای معرف؛
- تغییر لیدربورد به دریافت ده‌تایی، Scroll تا انتهای جدول و نمایش متمایز رتبه‌ی User فعلی؛
- افزودن Migration دوم و اعتبارسنجی واقعی همه‌ی جریان‌ها روی PostgreSQL موقت با پاک‌سازی کامل محیط تست.
- پارامتری‌کردن Image پایه‌ی Node برای Build تولید و امکان استفاده از Registry آروان به‌جای Docker Hub روی سرور ایران.
- جداسازی لایه‌ی Prisma/Migration در Image API برای جلوگیری از باقی‌ماندن Migration قدیمی در Cache انتشار.
- یکی‌کردن Image سرویس‌های API و Migration تا چرخه‌ی انتشار هیچ‌گاه Migration را با Image قدیمی اجرا نکند.

## ۲۰۲۶-۰۹-۰۷ — رفع Session ورود روی IP موقت

- جداکردن فلگ `Secure` کوکی Session از `NODE_ENV` با متغیر صریح `AUTH_COOKIE_SECURE`؛
- فعال‌کردن امکان QA ورود و بازی روی IP و HTTP، بدون تغییر پیش‌فرض امن Production؛
- افزودن تست مستقل برای Cookie موقت HTTP و Cookie امن HTTPS؛
- ثبت الزام بازگرداندن Cookie امن همزمان با اتصال دامنه و HTTPS.

## ۲۰۲۶-۰۹-۰۷ — زیرساخت Production و Auto-deploy

- افزودن Dockerfileهای مستقل Next.js و NestJS و Compose شامل PostgreSQL 17 و Caddy؛
- خصوصی‌کردن پورت‌های Web، API و Database و قراردادن Caddy به‌عنوان تنها ورودی عمومی؛
- افزودن اجرای Migration پیش از انتشار و Health Check پس از جایگزینی Containerها؛
- نصب OpenSSL داخل Image سرویس API و تأمین URL غیرواقعی Build برای اجرای پایدار Prisma Generate؛
- افزودن Timer یک‌دقیقه‌ای Server-side برای دریافت خودکار Commitهای تازه‌ی Branch `main`؛
- نصب Docker روی ابرک، فعال‌سازی UFW، اجرای Migration اولیه و تأیید پاسخ عمومی Web و API؛
- مستندسازی SSH، Environment، Domain، TLS، Backup و عملیات استقرار ابرک ابرآروان.

## ۲۰۲۶-۰۹-۰۷ — ساده‌سازی README

- بازنویسی README به زبان انگلیسی و محدودکردن آن به معرفی کوتاه بازی و فناوری‌های استفاده‌شده؛
- حذف راه‌اندازی، دستورها، عیب‌یابی، جزئیات امنیتی و سایر توضیحات عملیاتی از README طبق درخواست.

## ۲۰۲۶-۰۹-۰۷ — بازنویسی README پروژه

- تکمیل معرفی محصول، قابلیت‌های MVP، Stack و ساختار Monorepo؛
- مستندسازی راه‌اندازی PostgreSQL، Migration و اجرای همزمان یا جداگانه‌ی Web و API؛
- افزودن راهنمای دسترسی از گوشی روی شبکه‌ی مشترک، متغیرهای محیطی، دستورهای توسعه و عیب‌یابی خطاهای رایج؛
- افزودن هشدارهای OTP آزمایشی، Secretهای Production و محدودیت‌های امنیتی MVP.
- تکمیل قواعد Git برای حذف خروجی TypeScript، Secretهای محلی و Skillهای تولیدشده‌ی ابزارها از Commit.

## ۲۰۲۶-۰۹-۰۷ — حفظ Motion ضروری Gameplay

- افزودن Override هدفمند تا Countdown و خرچ/خروچ حتی با فعال‌بودن `prefers-reduced-motion` رشد نرم کامل خود را اجرا کنند؛
- ثابت نگه‌داشتن Combo و ادامه‌ی کاهش سایر Motionهای غیرضروری در این حالت؛
- ثبت این نکته که انتقال سایت به Server رفتار Media Query سمت Browser/OS را تغییر نمی‌دهد.

## ۲۰۲۶-۰۹-۰۷ — سازگاری با Reduced Motion اندروید

- شناسایی علت حذف همزمان Countdown و خرچ/خروچ در بعضی گوشی‌های شیائومی: پرش قانون عمومی `prefers-reduced-motion` به فریم نهایی شفاف Animation؛
- افزودن Fallback ثابت و قابل مشاهده برای Countdown، خرچ/خروچ و Combo، بدون نادیده‌گرفتن ترجیح کاهش Motion کاربر.

## ۲۰۲۶-۰۹-۰۷ — بازگشت افکت قبلی خرچ/خروچ

- بازگرداندن `kherech.webp` و `khoroch.webp` ثابت به Gameplay طبق بازخورد طراحی؛
- بازگرداندن Motion نرم CSS، زمان Sequence برابر ۲.۴ ثانیه و تأخیر ۴۲۰ میلی‌ثانیه‌ای «خروچ»؛
- خارج‌کردن MP4 و Animated WebPهای Scale2 از Runtime به‌دلیل زمینه‌ی مشکی و رفتار بازپخش نامطلوب.

## ۲۰۲۶-۰۹-۰۵ — رفع انباشت خرچ/خروچ

- افزایش سرعت Animated WebPهای Scale2 از ۳.۴۷ به ۲.۱۷ ثانیه؛
- کاهش فاصله‌ی شروع «خروچ» از ۴۲۰ به ۳۰۰ میلی‌ثانیه و حذف کل Sequence پس از ۲.۵۵ ثانیه؛
- محدودکردن نمایش به یک Sequence فعال؛ هر افکت تازه، نمونه‌ی قبلی و Timeout آن را جایگزین می‌کند تا انیمیشن‌های قبلی دوباره ظاهر نشوند.

## ۲۰۲۶-۰۹-۰۵ — شفافیت واقعی افکت‌های Scale2

- حذف روش Screen blend به‌دلیل باقی‌ماندن پس‌زمینه‌ی مشکی MP4 در بعضی Browserها؛
- استخراج فریم‌های Scale2، حذف مشکی با Chroma Key و تولید Animated WebPهای ۴۲۰×۴۲۰ با Alpha واقعی؛
- تغییر Runtime به Animated WebP تک‌پخش با مجموع حجم حدود ۱۱۰KB، بدون تغییر ترتیب و فاصله‌ی زمانی «خرچ → خروچ».

## ۲۰۲۶-۰۹-۰۵ — ویدئوهای جدید خرچ/خروچ

- جایگزینی WebP و انیمیشن CSS قبلی با `Kherech Scale2.mp4` و `Khoroch Scale2.mp4`؛
- بهینه‌سازی هر دو ویدئوی ۱۰۰۰×۱۰۰۰ به H.264 با ابعاد ۴۲۰×۴۲۰ و مجموع حجم حدود ۶۶KB؛
- حذف بصری زمینه‌ی مشکی MP4 با Screen blend و حفظ فاصله‌ی دوطرفه‌ی افکت‌ها؛
- Mount کردن «خروچ» با تأخیر ۴۲۰ میلی‌ثانیه برای شروع پخش واقعی پس از «خرچ» و افزایش عمر Sequence متناسب با مدت ویدئو.

## ۲۰۲۶-۰۹-۰۵ — اصلاح شدت نارنجی HUD

- محدودکردن گرادیان اعداد امتیاز و تایمر به طیف نارنجی روشن‌تر با رنگ اصلی `#EE9F35`؛
- کاهش تیرگی سایه‌ی اعداد و حذف Override رنگ صفر تا دقیقاً مشابه اعداد ۳، ۲ و ۱ Countdown نمایش داده شود.

## ۲۰۲۶-۰۹-۰۵ — رنگ نارنجی HUD

- تغییر رنگ اعداد امتیاز و تایمر از فیروزه‌ای به نارنجی برند `#F2B049` بر اساس نمونه‌ی ارسالی؛
- هماهنگ‌کردن آیکن و حاشیه‌ی تایمر با رنگ نارنجی و حفظ دورخط تیره برای خوانایی روی پس‌زمینه‌ی چوبی.

## ۲۰۲۶-۰۹-۰۵ — پس‌زمینه، Combo امتیازی و کنترل Burst

- افزودن پس‌زمینه‌ی چوبی `background.png` به زمین بازی پس از تبدیل به WebP حدود ۱۶KB؛
- تغییر رنگ HUD به سفید–فیروزه‌ای با دورخط تیره برای خوانایی روی زمینه‌ی قهوه‌ای؛
- افزودن صدای سه‌ثانیه‌ای جلزولز/Crunch همزمان با شمارش معکوس؛
- اعمال ضریب Combo معتبر سمت سرور: ۱.۵ برای سه‌تایی، ۲ برای چهارتایی و `n/2` برای تعدادهای بالاتر؛
- شفاف‌شدن متن Intro با اعلام ۱۰ امتیاز پایه برای هر برش؛
- افزودن `gestureId` به Hit telemetry و هماهنگ‌کردن Score فوری Client با نتیجه‌ی Server؛
- افزودن Validation Version 7 و Cooldown قطعی پس از Burstهای چندتایی؛
- بسته‌شدن کارت نتیجه با لمس فضای بیرون آن، از جمله روی موبایل؛
- افزایش پوشش تست واحد به ۱۱ تست برای Combo scoring و Burst cooldown.

## ۲۰۲۶-۰۹-۰۵ — Countdown، موزیک و Burst Spawn

- افزودن شمارش معکوس سه‌ثانیه‌ای پیش از شروع یک دقیقه‌ی Gameplay؛
- افزودن نسخه‌ی ۶۱ ثانیه‌ای و کم‌حجم `Ready, Set, Slice!` با Fade داخلی از ثانیه‌ی ۵۱ تا ۶۱؛
- شروع همزمان موزیک و بازی در صفر و توقف/ادامه‌ی موزیک همراه Pause؛
- افزودن Burstهای قطعی ۲ تا ۴ آیتمی با فاصله‌های کوتاه و Validation Version 6؛
- تغییر انیمیشن خرچ/خروچ به رشد کاملاً پیوسته از `scale(0)` بدون فریم آغازین قابل مشاهده.

## ۲۰۲۶-۰۹-۰۵ — Combo و پرتاب سریع‌تر

- افزایش سرعت اولیه‌ی پرتاب و افزودن Validation Version 5 با حفظ سازگاری Scheduleهای قبلی؛
- افزایش سهم و زاویه‌ی مسیرهای مورب چپ/راست با محدودکردن انتهای مسیر داخل محدوده‌ی افقی؛
- نرم‌تر و طولانی‌ترشدن ورود خرچ/خروچ از مقیاس بسیار کوچک؛
- افزودن افکت `COMBO nX` برای حداقل ۳ برش در یک Gesture و پنجره‌ی شناور ۳۰۰ میلی‌ثانیه‌ای؛
- انگلیسی‌شدن رقم‌های امتیاز و تایمر بازی.

## ۲۰۲۶-۰۹-۰۵ — فیزیک بالستیک و بازی یک‌دقیقه‌ای

- کاهش مدت بازی از ۱۸۰ به ۶۰ ثانیه و افزودن Validation Version 4 با حفظ Sessionهای قدیمی؛
- جایگزینی مکث مصنوعی اوج با مسیر سهمی پیوسته شبیه پرتاب واقعی و افزایش حرکت افقی؛
- کاهش تدریجی زمان پرواز و فاصله‌ی Spawn برای افزایش سختی در طول بازی؛
- نگه‌داشتن نیمه‌های بریده‌شده تا خروج کامل از پایین Canvas؛
- تبدیل Pause به توقف واقعی بازی و تایمر با سقف تجمعی ۳۰ ثانیه؛
- نصف‌کردن Cooldown افکت خرچ/خروچ به ۹۰۰ میلی‌ثانیه و افزایش فاصله‌ی دو نوشته از Target؛
- تغییر پس‌زمینه‌ی زمین بازی به طیف تیره‌ی نفتی برای کنتراست واضح‌تر اعداد زرد و خوراکی‌ها.

## ۲۰۲۶-۰۹-۰۵ — بهبود Gameplay و کنترل‌ها

- کاهش ارتفاع پرتاب تا کمی بالاتر از وسط صفحه، نرم‌ترشدن مسیر و افزودن مکث کوتاه در اوج؛
- افزایش مدت پرواز و نسخه‌بندی Schedule به نسخه‌ی ۳ با حفظ سازگاری نسخه‌های قبلی؛
- بازتولید هر سه مجموعه‌ی کامل/چپ/راست از Assetهای صحیح `Kamel`، `Chap` و `Rast`؛
- اصلاح اندازه‌ی واقعی نیمه‌های برش‌خورده و نرم‌ترشدن جدایی آن‌ها؛
- نمایش ترتیبی و طولانی‌تر «خرچ» سپس «خروچ» با Cooldown برای برش‌های نزدیک؛
- طراحی HUD واکنش‌گرای موبایل‌محور با بطری کنار امتیاز، اعداد کارتونی و تایمر سمت راست؛
- حذف کنترل قطع صدا و افزودن Pause، ادامه و پایان زودهنگام امن با اعتبارسنجی سمت سرور.

## ۲۰۲۶-۰۹-۰۵ — تغییر پورت توسعه‌ی Web

- انتقال پورت Web از `3000` به `3002` و API از `4000` به `4002` برای جلوگیری از تداخل با سایت دیگر؛ Host شبکه‌ی فعلی `10.215.216.104` برای Preview مجاز شد.

## ۲۰۲۶-۰۹-۰۴ — Core Gameplay MVP

- ایجاد Monorepo با Next.js، NestJS، Prisma و PostgreSQL؛
- افزودن ورود/ثبت‌نام OTP و Cookie Session؛
- افزودن بازی سه‌دقیقه‌ای Canvas با Asset، صدا و Particle؛
- افزودن اعتبارسنجی سمت سرور و محدودیت‌های اولیه‌ی Fraud؛
- افزودن بهترین امتیاز و لیدربورد؛
- بهینه‌سازی Assetهای تحویلی برای WebP؛
- افزودن Migration، تست واحد و مستندات پروژه.
- تثبیت Timezone اتصال PostgreSQL روی UTC برای جلوگیری از اختلاف زمان Session در سرورهای محلی.
- استفاده از ساعت Monotonic مرورگر برای حذف اثر اختلاف ساعت دستگاه روی Gameplay.
- تأیید End-to-end جریان OTP، Cookie Session، Game Session، Score و Leaderboard روی PostgreSQL و پاک‌سازی داده‌ی تست.
- مجازکردن `localhost` و `127.0.0.1` برای HMR و Preview محلی Next.js.
- مجازکردن IP فعلی شبکه‌ی توسعه برای تست مستقیم نسخه‌ی موبایل روی Wi-Fi مشترک.
- افزودن سمبوسه‌ی کامل و دو نیمه به Intro، Spawn، برش، Particle و اعتبارسنجی نسخه‌بندی‌شده‌ی سرور.
- تثبیت نسبت تصویر افکت خرچ/خروچ در Preview موبایل.
- تثبیت نسبت ابعاد تصاویر Responsive در رندر Next.js.
